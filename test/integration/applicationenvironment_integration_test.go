package integration

import "testing"

func TestApplicationEnvironmentIntegrationPlaceholder(t *testing.T) {
	t.Skip("integration test scaffold: run against envtest or kind in a dedicated integration pipeline")
}
