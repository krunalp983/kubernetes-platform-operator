{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubernetes-platform-operator.serviceAccountName" . }}
  labels:
    {{- include "kubernetes-platform-operator.labels" . | nindent 4 }}
{{- end }}
